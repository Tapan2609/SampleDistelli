tapan/SampleDistelli:

   Env:
    - CLASSPATH: 'target/*:lib/*'
    #
    # Set the JAVA_HOME below for the destination
    # deployment server JRE directory.
    #
    - JAVA_HOME: "/usr/lib/jvm/java-1.8.0-openjdk-amd64/jre"
    #
    - JVM_ARGS: '"-Duser.timezone=UTC -Xmx128M -Xms128M"'
    - PORT: "8001"
    - ARGS: "$PORT"

  Exec:
    - exec $JAVA_HOME/bin/java -cp $CLASSPATH $JVM_ARGS SampleDistelli $ARGS

  PkgInclude:
    - target/*.jar
    - lib/*.jar